public class UtilityTriggers
{
    //Variable used to store the names of triggers that were already executed in order to prevent triggers call themselves
    public static Set<String> executedTriggers = new Set<String>();
}